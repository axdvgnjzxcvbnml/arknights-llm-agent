name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHONUNBUFFERED: "1"

jobs:
  # 1) 语法检查：用 ast.feature_version=(3, 8) 在较新解释器上强制 Python 3.8 语法兼容
  syntax-check:
    name: 语法检查（Python 3.8 兼容）
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Python 3.8 语法兼容性检查
        run: |
          python - <<'EOF'
          import ast
          import pathlib
          import sys

          files = sorted(
              p for p in pathlib.Path(".").rglob("*.py")
              if ".git" not in p.parts and "__pycache__" not in p.parts
          )
          bad = []
          for p in files:
              try:
                  ast.parse(p.read_text(encoding="utf-8"), filename=str(p), feature_version=(3, 8))
              except SyntaxError as e:
                  bad.append((p, e))
          if bad:
              for p, e in bad:
                  print(f"语法错误 {p}:{e.lineno}:{e.offset} {e.msg}")
              print(f"{len(bad)} 个文件未通过 Python 3.8 语法检查")
              sys.exit(1)
          print(f"OK: {len(files)} 个文件通过 Python 3.8 语法检查")
          EOF

  # 2) 冒烟测试：mock 全链路，不依赖 GPU / 模拟器 / PRTS 数据
  smoke-test:
    name: 冒烟测试（mock 全链路）
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: syntax-check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: 安装冒烟测试最小依赖
        run: pip install -q --disable-pip-version-check numpy pydantic pyyaml rich pytest
      - name: 运行冒烟测试
        run: bash scripts/run_smoke.sh

  # 3) 单元测试：mock 用例恒跑；数据/真机/GPU 依赖用例自动 skip（不装 torch 等重包）
  pytest:
    name: 单元测试（mock 恒跑，重依赖自动 skip）
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: syntax-check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: 安装单元测试最小依赖（不含 GPU/重数据栈）
        # numpy/pydantic/pyyaml/rich：核心契约与渲染；networkx：知识图谱；
        # fastapi+httpx：API 与 TestClient。torch/transformers/chromadb/paddleocr/
        # ultralytics/mcp 等均在真实路径内懒加载，mock 用例不需要，故不安装。
        run: pip install -q --disable-pip-version-check numpy pydantic pyyaml rich networkx fastapi httpx pytest
      - name: 运行全量单元测试（缺数据/真机/GPU 的用例自动 skip）
        run: python -m pytest tests -q

  # 4) 环境检查
  env-check:
    name: 环境检查
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: syntax-check
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: 安装环境检查最小依赖
        run: pip install -q --disable-pip-version-check numpy pydantic pyyaml pytest
      - name: 运行环境检查
        run: bash scripts/check_env.sh
