# 核心运行依赖（V100 / CPU 共用）。版本为下限约束；pip 自动解析兼容版本。
# 安装：pip install -r requirements.txt
# 注意：torch 等重依赖不在这里强制；真实训练/推理按 docs/v100_checklist.md 单独装。

# 知识库
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
networkx>=3.0
pyyaml>=6.0

# RAG（构建与检索；bge 真实 embedding 可选，缺失自动回退 mock）
chromadb>=0.5.0
sentence-transformers>=2.7.0

# 服务
fastapi>=0.116
uvicorn>=0.30
httpx>=0.27
